# 📘 Python ミニ課題：簡易「JSON クエリフィルタ」ツールを作る

## 📝 説明
標準入力から **JSON 配列** を受け取り、  
その中から **特定キーの値が条件を満たす要素だけ** を出力するプログラムを作成してください。

JSON パースは `json` 標準ライブラリのみ使用できます。

---

## 🎯 仕様

- 標準入力で以下の2つが渡される：
  1. JSON 配列（例： `[{"name": "A", "score": 20}, ...]`）
  2. 条件式文字列  
     - 形式： `キー名 > 数値` または `キー名 == "文字列"`  
     - 例：  
       - `score > 50`  
       - `name == "Alice"`

- 条件式に一致した要素だけを **JSON として標準出力に返す**

---

## 🔍 入力例
```
[{"name": "A", "score": 20}, {"name": "Bob", "score": 85}]
score > 30
```

## 🔎 出力例
```
[{"name": "Bob", "score": 85}]
```

---

## 🔧 ヒント
- `eval` は **使わないこと**（危険なので禁止）
- 条件式のパースは自分で軽く実装すること  
  - 例：文字列の `>`, `<`, `==` を探す

---

## 🎉 目標
簡易的な **クエリフィルタ** を自作することで、  
文字列パース・JSON 操作・リスト内包表記の練習になります。

